'use strict';

(function () {
  var ESC_KEY = 'Escape';
  var ENTER_KEY = 'Enter';
  var CLICK_MOUSE_LEFT = 1;

  var MAIN_PIN_DEFAULT_POS = {
    left: '570px',
    top: '375px'
  };

  var HOUSE_TYPE = {
    bungalo: 0,
    flat: 1000,
    house: 5000,
    palace: 10000
  };

  window.const = {
    ESC_KEY: ESC_KEY,
    ENTER_KEY: ENTER_KEY,
    CLICK_MOUSE_LEFT: CLICK_MOUSE_LEFT,
    MAIN_PIN_DEFAULT_POS: MAIN_PIN_DEFAULT_POS,
    HOUSE_TYPE: HOUSE_TYPE
  };
})();
